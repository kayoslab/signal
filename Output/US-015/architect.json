{"approved":true,"adr_entry":"## ADR-015: Privacy Posture and Uniqueness Estimate on Receipt\n\n**Status:** Accepted  \n**Date:** 2026-04-24  \n**Ticket:** US-015\n\n### Context\n\nThe application needs to display a human-readable privacy posture summary and uniqueness estimate on the fingerprint receipt, derived from the entropy score computed in US-014. The `src/scoring/entropy-score.ts` (US-014) exports `calculateEntropyScore(snapshot): EntropyResult` with a `score` field (0–100 integer), `maxPossible`, and `breakdown`. The `src/modules/fingerprint/fingerprint-receipt.ts` (US-013, US-017) renders a receipt with 12 signal rows plus a rerun button. The `src/ui/receipt.ts` (US-011) provides `ReceiptRow` interface (`{ label: string; value: string }`) and `createReceiptRow(label, value)`. The `src/scoring/` directory contains two scoring modules (`entropy-score.ts`, `permission-debt-score.ts`) following the pattern of typed result interfaces with pure scoring functions.\n\n### Decision\n\nAdd a privacy posture computation module and integrate it into the fingerprint receipt:\n\n1. **Privacy posture function** (`src/scoring/privacy-posture.ts`) — pure function `computePrivacyPosture(result: EntropyResult): PrivacyPostureResult` returning entropy label (`'Low' | 'Moderate' | 'High'`), uniqueness estimate text, and privacy posture summary line, using threshold constants (LOW ≤33, MODERATE 34–66, HIGH ≥67)\n2. **Receipt extension** (`src/modules/fingerprint/fingerprint-receipt.ts`) — after computing entropy, call `computePrivacyPosture()` and append three new `ReceiptRow` entries: 'Entropy Level', 'Uniqueness Estimate' (with '[Heuristic]' suffix), and 'Privacy Posture'\n3. **Unit tests** (`tests/us-015/privacy-posture.test.ts`) — boundary conditions, label mapping, heuristic marker presence, determinism\n4. **Integration tests** (`tests/us-015/fingerprint-receipt-posture.test.ts`) — DOM verification of new rows\n\n### Architectural Impact Assessment\n\n**Scope:** Very small — adds one source file to `src/scoring/` (third scoring module after `entropy-score.ts` and `permission-debt-score.ts`), modifies one existing file (`src/modules/fingerprint/fingerprint-receipt.ts`), and adds one new test directory (`tests/us-015/`). No new dependencies. No build pipeline changes. No configuration changes.\n\n**New dependencies:** None. The module imports only the `EntropyResult` type from `src/scoring/entropy-score.ts`. No runtime or dev dependencies are added.\n\n**Build pipeline change:** None. Existing `npm run lint`, `npm run typecheck`, and `npm run test` scripts cover all new and modified files without modification.\n\n**Risks identified and mitigations:**\n\n- **Existing US-013 test asserts exactly 12 `.receipt-row` elements:** The test at `tests/us-013/fingerprint-receipt.test.ts:91` asserts `expect(rows.length).toBe(12)`. Adding 3 new receipt rows changes the count to 15, which **will break this test**. The implementation must update the US-013 test assertion from 12 to 15. This is the highest-impact consequence of this change — not architecturally significant, but a concrete regression that must be addressed during implementation.\n\n- **US-017 rerun flow must also recalculate posture:** The current `fingerprint-receipt.ts` rerun callback (lines 19–29) recollects the snapshot and repopulates `.receipt-rows`. The modified version must also recalculate entropy score and privacy posture on rerun, then include the three new rows in the repopulated container. If the rerun callback only regenerates the original 12 `formatSnapshotToRows` rows without posture rows, the posture data will disappear after rerun. The implementation must ensure the full row set (12 signal rows + 3 posture rows) is rebuilt on each rerun.\n\n- **Threshold values (33/66) are arbitrary:** The plan correctly identifies this and uses named constants for easy adjustment. The thresholds map to the 0–100 entropy score: ≤33 is Low (most signals missing/blocked), 34–66 is Moderate, ≥67 is High (most signals present and distinguishing). These are reasonable ordinal boundaries but have no empirical backing. Keeping them as exported constants enables future tuning without API changes.\n\n- **Uniqueness estimate text (e.g., '~1 in 1,500 browsers') has no statistical backing:** The plan requires this text to always carry a '[Heuristic]' suffix, which directly satisfies the acceptance criterion \"Estimate clearly labeled as heuristic\" and aligns with CLAUDE.md content rules (\"never claim certainty where inference is used\"). The estimate text is a fixed string per band, not a calculated statistical probability — this is the appropriate approach given the lack of a reference dataset.\n\n- **`computePrivacyPosture` depends on `EntropyResult` interface stability:** The `EntropyResult` interface is simple (`score`, `maxPossible`, `breakdown`) and `computePrivacyPosture` only needs the `score` field. This is a minimal, stable dependency. If `EntropyResult` is extended with optional fields in future tickets, `computePrivacyPosture` is unaffected.\n\n- **Placement in `src/scoring/` vs `src/modules/fingerprint/`:** The plan places the posture function in `src/scoring/`, which is appropriate — it is a pure scoring/classification function with no DOM logic, consistent with CLAUDE.md's mandate \"never mix DOM logic into scoring modules.\" The fingerprint receipt module orchestrates the call, keeping the separation of concerns intact.\n\n- **Receipt row ordering:** The three new rows (Entropy Level, Uniqueness Estimate, Privacy Posture) should appear after the 12 signal rows, creating a natural flow from raw data to derived assessment. The plan's approach of appending rows to the existing array achieves this.\n\n- **If fingerprint receipt is not yet mounted in `main.ts`:** The plan's risk section correctly notes this. The `renderFingerprintReceipt` function is exported and available but may not be wired into the app bootstrap. The posture rows will exist in the receipt element regardless of whether it is currently mounted — this is out of scope for US-015 and does not affect the implementation.\n\n**Consistency with existing architecture:** Fully consistent. The privacy posture function follows the established `src/scoring/` pattern: typed result interface, pure function, threshold constants with clear semantics. The receipt extension follows the existing `fingerprint-receipt.ts` orchestration pattern: collect data → compute → format → render. The '[Heuristic]' labeling aligns with the `INFERENCE_MARKER` pattern established in `src/modules/shadow-profile/inference-schema.ts` (US-024), though it uses inline text rather than the formal marker constant — appropriate since this is a receipt row value, not a structured inference statement. Tests follow the `tests/us-{number}/` convention. No runtime dependencies added. All computation is client-side per trust rules.\n\n**No breaking changes** to existing build pipeline or deployment. The US-013 row count test requires a trivial update (12 → 15).\n\n### Consequences\n\n- **Positive:** Translates the raw entropy score (US-014) into human-readable privacy posture information, making the fingerprint receipt actionable for non-technical users\n- **Positive:** Pure function in `src/scoring/` maintains clean separation between scoring logic and DOM rendering\n- **Positive:** Threshold constants enable easy tuning of posture bands without code restructuring\n- **Positive:** '[Heuristic]' suffix on uniqueness estimate satisfies CLAUDE.md content rules and acceptance criteria\n- **Positive:** Zero new dependencies keeps the bundle lightweight per CLAUDE.md constraints\n- **Positive:** All computation remains client-side; no trust rule violations\n- **Negative:** Breaks existing US-013 test (row count assertion 12 → 15) — must be updated during implementation\n- **Negative:** Rerun callback in `fingerprint-receipt.ts` must be extended to include posture rows, adding complexity to the rerun flow\n- **Trade-off:** Fixed estimate text per band (not calculated probability) is less precise but avoids false statistical authority — the right trade-off given CLAUDE.md's content rules against claiming certainty"}
