{"approved":true,"adr_entry":"## ADR-003: Static Local-Only Architecture Policy\n\n**Status:** Accepted  \n**Date:** 2026-04-23  \n**Ticket:** US-003\n\n### Context\n\nThe project's core trust contract (CLAUDE.md) mandates no cookies, no analytics, no tracking, no backend profile creation, and all analysis running locally in-browser. Currently there are no automated guardrails to enforce these constraints — a developer could inadvertently add `google-analytics`, `segment`, or a backend framework like `express` without any CI-time or build-time check catching it.\n\n### Decision\n\nIntroduce a multi-layered architecture policy enforcement system:\n\n1. **Policy definition** (`src/policies/architecture-policy.ts`) — centralized banned dependency patterns and validation logic\n2. **Build-time validation script** (`scripts/validate-architecture.ts`) — checks package.json and Vite config against policy, wired into build via `npm run policy:check && vite build`\n3. **Static analysis via ESLint** — `no-restricted-imports` rule banning analytics/tracking modules; new `npm run lint` script (required by CLAUDE.md but previously missing)\n4. **Test-time verification** — unit tests for policy logic, integration tests asserting dist/ contains only static assets, and build-output scanning for tracking script references\n\n### Architectural Impact Assessment\n\n**Scope:** Medium — adds new directories (`src/policies/`, `scripts/`, `tests/us-003/`), new devDependencies (eslint, @typescript-eslint/*, tsx), and modifies build pipeline.\n\n**New dependencies:** ESLint + TypeScript parser/plugin (devDependencies only), tsx (devDependency for running validation script). These are standard tooling dependencies that don't affect the runtime bundle.\n\n**Build pipeline change:** The `build` script gains a policy pre-check gate (`npm run policy:check && vite build`). This adds a few seconds to builds but prevents privacy-violating dependencies from shipping.\n\n**Risks identified and mitigations:**\n- ESLint devDependencies must be excluded from the policy checker's runtime dependency scan (the plan acknowledges this — policy should check `dependencies` vs `devDependencies` separately)\n- `no-restricted-imports` only catches static imports; build-output scanning (step 8) covers the dynamic import gap\n- The banned list requires ongoing maintenance but is documented and extensible\n- `tsx` adds a devDependency; acceptable for tooling scripts\n\n**Consistency with existing architecture:** Aligns well — project already uses Vite for static output, has Vitest for testing, and the plan fills gaps noted in CLAUDE.md (missing `npm run lint` and `npm run test` scripts). No runtime code changes; all additions are tooling/policy layer.\n\n**No breaking changes** to existing code or tests. The US-001 build-output test pattern is consistent with the new static-deploy integration test approach.\n\n### Consequences\n\n- **Positive:** Automated enforcement of the project's trust contract; future dependency additions are gated; lint and test scripts now match CLAUDE.md requirements\n- **Positive:** Multi-layered defense (dependency check + import lint + build output scan) provides defense-in-depth\n- **Negative:** Adds ~4 devDependencies and build-time overhead; banned list needs periodic updates\n- **Trade-off:** Custom ESLint rule for fetch/XHR external endpoint detection may be fragile — build-output scanning is the more reliable backstop"}
